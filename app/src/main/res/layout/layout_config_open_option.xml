<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content" >

    <ImageView
        android:id="@+id/imageview_open_option_icon"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:background="@drawable/background_rounded_white_10_100"
        android:padding="7dp"
        android:src="@drawable/ic_dark_mode"
        app:tint="@color/secondary" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginStart="20dp"
        android:layout_toEndOf="@id/imageview_open_option_icon"
        android:layout_toStartOf="@id/imageButton_open_option"
        android:orientation="vertical" >

        <TextView
            android:id="@+id/textView_open_option_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="marquee"
            android:maxLines="1"
            android:text="Night Mode"
            android:textColor="@color/white"
            android:textSize="13dp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/textView_open_option_subtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="marquee"
            android:maxLines="2"
            android:text="Permite que o flowbrew mostre notificações mostrando os status"
            android:textSize="12dp" />
    </LinearLayout>

    <ImageButton
        android:id="@+id/imageButton_open_option"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:background="@android:color/transparent"
        android:importantForAccessibility="no"
        app:srcCompat="@drawable/ic_navigate_next" />

</RelativeLayout>